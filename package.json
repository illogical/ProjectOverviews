{
  "name": "project-overviews",
  "version": "1.0.0",
  "description": "Gather, index, and summarize project documentation across a software ecosystem",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
